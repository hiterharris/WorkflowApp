import { StyleSheet } from "react-native"

export default StyleSheet.create({
    timer: {
        alignItems: 'center',
    },
    title: {
        fontSize: 24,
        fontWeight: '600',
        color: 'white',
      },
    inputs: {
        flexDirection: 'row',
    },
    time: {
        color: 'white',
        fontSize: 48,
    },
});
